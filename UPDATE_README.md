# 🔧 更新包：管理員後台

## 📅 更新日期：2026-01-10

## 📦 包含檔案

```
main.py                           # 覆蓋（加入 /admin 路由）
app/
├── main.py                       # 覆蓋（註冊 admin API）
├── core/
│   └── security.py               # 覆蓋（加入 require_admin）
└── api/v1/
    ├── __init__.py               # 覆蓋（導出 admin_router）
    └── admin.py                  # 新增（管理員 API）
static/
├── dashboard.html                # 覆蓋（加入管理後台連結）
└── admin.html                    # 新增（管理員後台頁面）
```

## 🚀 部署步驟

```bash
# 1. 解壓縮到專案根目錄（覆蓋）

# 2. 推送
git add .
git commit -m "feat: 新增管理員後台"
git push

# 3. 等待 Railway 部署完成
```

## ✨ 功能說明

### 📊 系統總覽
- 總用戶數、管理員數
- 系列團統計（進行中/總數）
- 總資金池金額

### 👤 用戶管理
- 查看所有用戶列表
- 搜尋用戶（依名稱）
- 篩選角色（管理員/一般用戶）
- 變更用戶角色（設為管理員或移除）

### 🎰 系列團管理
- 查看所有系列團
- 篩選狀態（進行中/已結束）
- 顯示成員數、資金池、期數

### 📋 事件日誌
- 查看系統事件記錄
- 建立系列團、成員加入、開新期等

## 🔐 權限控制

- 所有 `/api/v1/admin/*` API 需要管理員權限
- 前端頁面 `/admin` 會自動檢查權限
- 非管理員訪問會被導回儀表板

## 📱 使用方式

1. 登入系統（需為管理員）
2. 儀表板會顯示「🔧 管理後台」按鈕（紫色）
3. 點擊即可進入管理後台
4. 或直接訪問 `/admin`

## 🎨 儀表板變更

- 新增「管理後台」快速操作按鈕
- 僅管理員可見（紫色樣式）
- 自動根據用戶角色顯示/隱藏

## 🔗 新增 API 端點

| 方法 | 路徑 | 說明 |
|------|------|------|
| GET | `/api/v1/admin/stats` | 系統統計 |
| GET | `/api/v1/admin/users` | 用戶列表 |
| GET | `/api/v1/admin/users/{id}` | 用戶詳情 |
| PUT | `/api/v1/admin/users/{id}/role` | 變更角色 |
| GET | `/api/v1/admin/series` | 系列團列表 |
| GET | `/api/v1/admin/logs` | 事件日誌 |

## ⚠️ 注意事項

- 管理員不能修改自己的權限（防止誤操作）
- 至少保留一個管理員帳號
